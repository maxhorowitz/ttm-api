from setuptools import setup, find_packages

setup(name='ttmquant',
      version='0.1',
      author='Max Horowitz',
      url='https://github.com/maxhorowitz/ttm-api',
      packages = find_packages(),
)
syntax = "proto3";

package quant_service;

option go_package = "proto/";

service QuantService {
    rpc Regression(RegressionRequest) returns (RegressionResult) {}
}

message RegressionRequest {
    string id = 1;

}

message RegressionResult {
    string id = 1;
    bool submitted = 2;
}